<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="springboot.demo.mapper.UserMapper">
    <resultMap id="userResultMap" type="springboot.demo.model.User" >
	    <result column="id" property="id" jdbcType="BIGINT" />
	    <result column="userName" property="userName" jdbcType="VARCHAR" />
	    <result column="passWord" property="passWord" jdbcType="VARCHAR" />
	    <result column="user_sex" property="userSex" jdbcType="VARCHAR"/>
	    <result column="nick_name" property="nickName" jdbcType="VARCHAR" />
    </resultMap>
    
    <sql id="columns" >
         id, userName, passWord, user_sex, nick_name
    </sql>
    
    <insert id="addUser" parameterType="springboot.demo.model.User" useGeneratedKeys="true" keyProperty="id">
        insert into users
        (userName, passWord, user_sex, nick_name)
        values
        (#{userName},#{passWord},#{userSex},#{nickName})
    </insert>
    
    <update id="updateUser" parameterType="springboot.demo.model.User">
        update users
        <set>
            <if test="userName != null and userName != ''">
                userName = #{userName},
            </if>
            <if test="passWord != null and passWord != ''">
                passWord = #{passWord},
            </if>
            <if test="nickName != null and nickName != ''">
                nick_name = #{nickName},
            </if>
            <if test="userSex != null and userSex != ''">
                user_sex = #{userSex},
            </if>
        </set>
        where id = #{id}
    </update>
    
    <delete id="deleteUserById" parameterType="java.lang.Long">
        delete from users where id = #{id}
    </delete>
    
    <select id="findUserList" resultMap="userResultMap">
	   SELECT 
	   <include refid="columns" />
	   FROM users
	</select>
	
	
    
</mapper>